---
applyTo: "*.ts"
---
Do not use semicolons (;) at the end of statements except for the following cases:
- return statements
- break statements
- continue statements

Use single quotes (') for strings instead of double quotes (") unless the string contains a single quote character, in which case you can use double quotes.